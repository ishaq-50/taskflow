{
  "buildCommand": "npm install --prefix client && npm install --prefix server && npm run build --prefix client && npm run build --prefix server",
  "outputDirectory": "client/dist",
  "env": {
    "NODE_ENV": "production"
  }
}
